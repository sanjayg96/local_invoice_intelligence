# Kleister Adapter

Kleister Charity/NDA is a better fit for general key information extraction than invoice-only benchmarks.

The current adapter uses the same normalized layout as SROIE:

```text
dataset-root/
  pdfs/
  annotations/
```

Keep field names in your YAML/JSON field config aligned with the annotation keys you want to score.
